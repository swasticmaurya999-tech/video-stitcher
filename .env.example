# ── Copy to .env and fill in. All values have sensible defaults except cloud keys. ──

# --- Storage backend: "local" (default, no keys) or "r2" ---
STORAGE_BACKEND=local
# R2 (only needed when STORAGE_BACKEND=r2)
R2_ENDPOINT=
R2_ACCESS_KEY=
R2_SECRET_KEY=
R2_BUCKET=video-stitcher
PRESIGN_TTL=3600

# --- LLM planners (failover chain). Empty keys are skipped → heuristic fallback. ---
PLANNER_CHAIN=gemini,groq,heuristic
GEMINI_API_KEY=
GROQ_API_KEY=
LLM_TIMEOUT=30

# --- Limits ---
MAX_FILES=50
MIN_FILES=1
MAX_FILE_SIZE_MB=100
MAX_TOTAL_SIZE_MB=1024
MIN_OUTPUT_SEC=10
MAX_OUTPUT_SEC=120
CLIP_SECONDS=3
MIN_CLIP=1.0

# --- Output profile ---
TARGET_WIDTH=1920
TARGET_HEIGHT=1080
FPS=30
ASPECT=16:9            # 16:9 | 9:16 | 1:1

# --- Feature toggles (trade quality <-> compute; each is wired) ---
ENABLE_CLIP=false        # CLIP visual tags (requires requirements-ml.txt)
ENABLE_DETECT=false      # YOLO object/person detection (requires requirements-ml.txt)
ENABLE_WHISPER=true      # speech transcription + speech-aware cuts
ENABLE_STABILIZE=false   # ffmpeg deshake on shaky clips
ENABLE_MUSIC=true        # background music bed under the output (ad soundtrack)
MUSIC_PATH=              # custom track path; empty = bundled app/assets/music.mp3
MUSIC_VOLUME=0.35        # 0..1 music level under clip audio

# --- Analysis bounding ---
WHISPER_MODEL=base
ANALYZE_FPS=1.0
ANALYZE_MAXDIM=480
MAX_SEGMENTS_CONSIDERED=120

# --- Worker / lifecycle ---
WORKER_CONCURRENCY=1
MAX_JOB_ATTEMPTS=2
UPLOAD_TTL_H=24
OUTPUT_TTL_H=24
SEGMENT_TTL_H=24
DISK_MIN_FREE_MB=500
JANITOR_INTERVAL_SEC=3600

# --- Paths ---
DATA_DIR=./data
DB_PATH=./data/app.db
TEMP_DIR=./data/tmp
