# Optional heavy ML deps — install ONLY if enabling ENABLE_CLIP (visual tags) or ENABLE_DETECT
# (object/face detection). These pull in torch (multi-GB). Whisper does NOT need these.
#   pip install -r requirements-ml.txt
open-clip-torch==2.30.0
ultralytics==8.3.55
pillow==11.1.0
torch==2.5.1
